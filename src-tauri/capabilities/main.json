{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Only the core APIs needed by the main application window.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
